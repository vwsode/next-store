import React, { FC } from 'react';

interface Props {
    title: string;
    value: string;
}

const SelectOption: FC<Props> = ({ title, value }) => {
    return <div>SelectOption</div>;
};

export default SelectOption;
